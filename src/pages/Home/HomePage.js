import React, { useRef, useEffect, useState } from "react";
import "./home.css";

const HomePage = (props) => {
  return (
    <div>
      <code>
        Flappy-Bird learns gets smart with a little help from Neural-Networks and Genetic Algorithim
      </code>
    </div>
  );
};

export default HomePage;
